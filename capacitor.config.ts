import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.silentturtlehumcopy',
  appName: 'silent-turtle-hum-copy',
  webDir: 'dist'
};

export default config;
